# 审计证据与影响范围

阅读范围：固定提交 `73996528634ffe0362f1a0341771658171f3b5e2` 的关键科学文档、近期提交与核心执行代码。这里的“确定”指在读到的代码/记录中可直接定位，不表示已用真实数据复现其效应量。

| ID | 类型与定位 | 事实/问题 | 影响与最低动作 |
|---|---|---|---|
| A01 | 确定；STATUS/TODO vs D-124–127 | 最新 Lane A/B 已完成，入口仍写待做 | 更新工作面；不重复开工 |
| A02 | 确定；`scripts/r16_axis_factory.py::main`，cross-patient matching | `sim=nrm@nrm.T` 后直接聚类，未取绝对值；mean_load 未符号对齐 | D-118 个体 PC 复现数需重算；仅 loading 匹配，无需新 GPU |
| A03 | 确定；同脚本 novelty 逻辑 | 用各轴相关的最大值判断 NEW | 已知轴线性组合可被误判新；补联合解释力 |
| A04 | 已知方法限制；LEADS L-008、R16 design | 逐根 PC 不能识别跨样本基底旋转 | 有边界子空间诊断，不许据此自动推断新场存在 |
| A05 | 确定；`scripts/r16_laneA_screen.py::main` 的 med/height/null_h | cohort 汇总峰值与单片峰值混合比较 | D-126 的普遍否证外推不成立；同统计量重算 |
| A06 | 确定；同脚本 peaks/agree | 一致率按切片，不是患者；n_patients 是可用患者数 | 多片患者多票；记录真正支持患者及有效箱 |
| A07 | 确定；同脚本 peak_height/peak_agree | 只有正向 argmax 及固定峰位条件，非原定完整函数族 | 不能概括无连续函数；补完整曲线/负向/宽峰 |
| A08 | 确定；`r16/census.py::spatial_null_remap` | 最近邻可多对一，best-effort 可不达接受线 | 不可称完整置换；保存近似性质与校准 |
| A09 | 确定；Lane A section_profiles 的 `[0]` | 使用 remap 索引但丢弃 info | 无法从结果确认接受与失真；补诊断流 |
| A10 | 确定的数学性质；`r16/axes.py::contour_stability` | 同分数嵌套 q mask 的 Jaccard 由分位面积决定 | 0.7083 不是独立鲁棒性证据；用分数扰动后稳定性 |
| A11 | 确定；`r16/axis_factory.py::lisa_tile_stats` | 两侧绝对 Local Moran 显著点合并，含不同符号局部结构 | 不要统一叫 high-high hotspot；相关 tile p 只作描述，尤其已平滑输入 |
| A12 | 确定语义差异；`axis_factory.py::augment` vs BANKSY | 工厂是矩阵相加，非 product-space concat | 改为自定义平滑名称；不是重跑全部臂的理由 |
| A13 | 确定实现；`scripts/r16_laneB_screen.py` | OLS 同片拟合后直接置换残差；50 draws，未作全家族正式校准 | 保留 191 为候选，正式条件 p 需校准；非191全假或全真 |
| A14 | 待查证上游；`census.py::log1p_zscore` | 本函数没有 spot 深度处理；缓存上游语义尚未在本次审阅中完全核定 | 必须先查 manifest/构建器；不能先宣布实际深度混杂 |
| A15 | 确定风险；Lane A/B axis_scores 与全基因测试 | 未按待测 g/模块重建排除重叠后的轴 | 筛查中有自包含途径；区分 marker 自证和独立读出 |
| A16 | 科学判断；D-127 vs plan H-03 | “状态/亚型”一概成为排除条件，可能排除目标本身 | 改为身份/状态/技术/空间信息分层，不降低证据要求 |
| A17 | 确定；D-124 | Harmony 共嵌入 ID 与 GraphST 逐片匹配口径不同 | 78/83 和32/356不是方法优劣或独立复现强度比较 |
| A18 | 确定；Lane B 保存逻辑 | coherent_patients 仅截取前10；I中位仍按切片 | 新输出保存完整患者表；显示权重与统计层次 |

## 这次没有据此断言的事情

没有断言 191 个候选全部来自测序深度；没有断言修复符号后一定能找回 NEW；没有断言正确 null 一定使 CXCL13 显著；没有断言有 activity-compatible 程序就证明细胞内状态机制；没有断言所有现有负结果都应撤销。

R-04 历史关闭、D-126 当前统计对象失配、D-127 候选尚未外部验证，是三种不同问题，必须分开处理。

## 本包独立微型复核

`diagnostics/audit_reproductions.py` 实现 7 组人工示例，12 个单元测试已通过：PC 符号、PC 旋转、已知轴混合、嵌套等高线、Lane A 统计量聚合、患者权重、最近邻映射非一一性。

结果见 `toy_audit.json`。例如无并列的 q0.6/0.7/0.8 mask，平均 Jaccard 0.708333；将原分数任意打乱仍相同。人工示例中的 p 值只是反例，不是患者检验值、功效评估或生产 null 的校准成绩。
