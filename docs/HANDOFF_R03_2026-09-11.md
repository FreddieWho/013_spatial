# Handoff：R-03 普查＋外部数据侦察冻结（2026-09-11）

> 目标读者：未来恢复 R-03/R-11 的 agent 或用户。只读本文档＋所列产物即可恢复全部上下文，无需翻聊天记录。
> 状态：**已冻结，可随时恢复**。R-03 普查完成；外部侦察完成；下载落地一半（Cervilla CRC 对）；待用户在 A/B/C/D 四选项中决策后继续。

## 1. 一句话结论

- 本地 22 个多切片块**零对已知片间距** → R-11 三类邻片任务 NOT_MEASURABLE，I-002 对 R-11 已升级 HARD_BLOCKED（roadmap 与 ISSUES 已记）。
- 外部侦察结论：Tier-1（同平台 Visium 邻片＋书面间距）**不存在**；最强候选是 Cervilla 六癌种（书面三连切 7µm，开放许可，已落地 CRC 对一半）与 HTAN WUSTL（15 块 48 片，dbGaP 受控）。
- H-06 保持 UNJUDGED（数据边界事实，非假设失败）。

## 2. 范围与完成判据对照（roadmap R-03）

| 判据要求 | 落点 |
|---|---|
| 只普查已下载数据，不新增下载 | ✅ 普查零下载；侦察阶段下载 Cervilla 对经用户明确批准（"如果你能下载就下载"） |
| 形成组织块级清单 | ✅ `infra/r03/serial_section_census_20260911.json`（167 单元／31+7+1+8 块结构，见 §4） |
| 区分四类任务可测性 | ✅ 同平面遮蔽 MEASURABLE，其余三类 NOT_MEASURABLE |
| 关键字段缺失则明确不可测项 | ✅ z_position／section_order 全注册表为空；HEST z_step 系采集步进（I-002 原判） |
| 是否申请新数据待另行决策 | ⏳ 即 A/B/C/D 四选项（§7），用户未定 |

## 3. 产物清单（canonical surface）

| 路径 | 内容 | 版本状态 |
|---|---|---|
| `infra/r03/serial_section_census_20260911.json` | 普查机读产物（schema `r03.serial_section_census.v1`，status `R03_CENSUS_COMPLETE`，含输入 sha256） | 待提交（untracked） |
| `scripts/r03_serial_section_census.py` | 普查脚本（只读 registry＋本地元数据） | 待提交（untracked） |
| `tests/test_r03_serial_section_census.py` | 1 项测试（仅同平面可测） | 待提交（untracked） |
| `data/other_sources/cervilla_2026/` | Cervilla CRC 对 zips＋解包目录（**git-ignored**，见 `.gitignore:/data/*`） | 本地唯一副本 |
| `docs/roadmap.md`（R-03 条目＋09-11 日期条） | 普查结论已记 | 待提交（modified） |
| `docs/ISSUES.md`（I-002） | R-11 范围 HARD_BLOCKED＋影响已记 | 待提交（modified） |
| `docs/decisions.md`（D-111） | 数据申请批准＋侦察标准＋下载另批规则 | 待提交（modified） |
| `TODO.md` | #31/#32/#33 已完成；R-03 行＋变更记录 | 待提交（modified） |

## 4. 普查关键数字（产物重算可得）

- 注册表 167 合格单元：HTAN 47 单元／31 块／**14 多片块**（2–3 片）；ST-CRC 14／7 块／**7×2**（README 明示 serial）；TENX Block-A 2／1 块／**1×2**；USZ 8 单片块（厚度 "5 um" 注册表来源）；external_geo 96 行无块信息。
- z_position／section_order：全注册表覆盖 0。厚度：仅 USZ 8 行。
- 配准输入：ST-CRC 与 Block-A 逐片 H&E hires＋scalefactors＋tissue positions 齐；HTAN／USZ 只有 h5ad 无 H&E；跨片配准误差本地未测量。
- 合计多切片块 22，**已知间距对数 0**。

## 5. 外部候选分级（侦察结论，下载前状态）

### Tier-1.5a：Cervilla 六癌种（已落地一半 ✅）

- 论文：Cervilla 等，Genome Biology 2026，DOI `10.1186/s13059-026-03937-y`（Europe PMC 全文已逐字核实，本地 `/tmp` 副本丢失可重下）。
- 书面方案：每块**三张连切 7µm**（Xenium、CosMx、Visium-v2 各一）＋一张 H&E；Visium v1 系半年前切（**不相邻**）；六瘤种各一块原发未治肿瘤（含结直肠腺癌）。
- 数据：Zenodo record `17999961`（Visium 套装，**CC-BY-4.0，开放**，15 文件 26GB）；Xenium/CosMx 在 `17986017`；XeniumMT/5K 在 `18000256`。分析代码：`github.com/scervilla/SpatialBenchmarking`。
- 已落地（`data/other_sources/cervilla_2026/`，md5 已对 Zenodo 记录验对）：
  - `visium_Colorectal.zip`（41.9MB，md5 `04e61cd93686dd828108839fc9bc10f0`）：4763 spot × 17941 基因，H&E hires＋scalefactors＋positions 齐。
  - `cytassist_Colorectal.zip`（62.7MB，md5 `5baf398d2602fb1b2c431fec5ba33dc3`）：3517 spot × 18087 基因；Sample ID `Colorectal_cyt_2_1`，V4 FFPE v2 chemistry，探针集 v2.0，spaceranger 2.1.1，作者手工配准文件 `colorectal_alignment.json`。
  - 共同基因 **17,367**。解包后共 212MB。
- **缺口**：triplet 内部顺序（CytAssist 邻居是 Xenium 还是 CosMx）无公开文本；Xenium CRC（`xenium_Colorectal.zip`，1.2GB，同许可开放）可下但下之前解不开邻接关系。
- Caveats：FFPE vs 我方 fresh-frozen；邻片对跨平台；Visium v1 那片不相邻。

### Tier-1.5b：HTAN WUSTL（Mo, Nature 2024，未下载 ⏳）

- 论文：Mo 等，Nature 2024，DOI `10.1038/s41586-024-08087-4`，PMID `39478210`，PMCID `PMC11525187`（开放获取）。
- 书面方案：15 个块 48 片，U1/U2/U3…编号，**片间距 5–100µm 范围**（逐对值待元数据）；Visium，人瘤含 CRC／BRCA／PDAC／CHOL 等；PASTE2 配准先例＋公开代码（`ding-lab/mushroom`，`ding-lab/ST_subclone_publication`）。
- 获取：**dbGaP 受控 `phs002371.v3.p1`**（需机构签字＋数周审批）；入口 `data.humantumoratlas.org` → WUSTL Atlas，用 ID 映射表定位文件。
- ID 映射表已取（公开 GitHub，本地 `/tmp/stsub/Data_access/Sample_ID_Lookup_table_v1.xlsx` —— **/tmp 会丢**，恢复时重下：`git clone --filter=blob:none` 该仓库后取 `Data_access/` 下文件；表结构：7 个 sheet，sheet2 为 Visium（Assay／Cancer／WUSTL ID／HTAN ID如 `HTA12_xxx`）；FFPE，Caveat 同上。
- 如用户选 C，先准备 dbGaP 申请材料，不直接开下。

### Tier-2：STDS0000224（未下载）

- STOmicsDB，人脑 4 对连切 Visium（"consecutive" 书面，数字间距未写）。同平台错组织 → 仅方法验证。下载条款恢复时再查。

### Tier-3（归档，不追）

- HER2+ 乳腺 Zenodo `4751624`／`3957257`（CC-BY-4.0）：同瘤多片但**无连切／间距记录**（只写 16µm 厚），且老 ST 平台 100µm 点。
- 3D_lung（rajewsky-lab）：CosMx 6 片（04–34 步 6），无 Visium，记录号与许可未找到。
- GSE251926（Open-ST）：淋巴结 19 片但平台完全不对，编号≠间距。
- 10x Block-A Sec1/Sec2：平台／组织／prep 全匹配、零下载，但间距无书面来源（官网机器人墙 429＋Vercel 21，镜像只写厚度 10µm）。可选项 D（邮件问 10x support）仍有效。

## 6. 待决策四选项（用户原文，恢复后直接问）

- **A**：下 Xenium CRC 1.2GB，凑 probable 邻片对先跑配准可行性（顺序按假设标注，不当证据）。
- **B**：起草邮件问 Cervilla 通讯作者（`scervillag@carrerasresearch.org`，抄送 `eporta@carrerasresearch.org`）triplet 顺序和逐对间距。
- **C**：启动 HTAN dbGaP 申请（慢功夫，先备材料）。
- **D**：起草邮件问 10x support（Block-A Sec1/Sec2 是否连切、间距多少）。

## 7. 恢复步骤（按选项）

- 通用：`git pull` → 确认 `data/other_sources/cervilla_2026/` 两 zip md5（§5）→ 跑 `PYTHONPATH=. python3 scripts/r03_serial_section_census.py` 复现普查产物。
- A：`curl -o xenium_Colorectal.zip https://zenodo.org/api/records/17986017/files/xenium_Colorectal.zip/content` → 对 record API 的 md5 → 解包验 H&E／counts／坐标 → 写配准可行性报告（顺序标假设）。
- B／D：起草邮件→用户发出（对外联系用户把关）。
- C：dbGaP `phs002371.v3.p1` 申请＋机构签字流程；并行重下 ID 映射表。
- 任选落地后：更新 roadmap R-11 判定（MEASURABLE 或维持）→ I-002 状态→ push。

## 8. 变更／审计记录

- 2026-09-11：R-03 普查完成（脚本＋产物＋1 测试）；I-002 对 R-11 升级 HARD_BLOCKED（满足"现有数据均无法建立可审计相邻切片关系"条件，已显式报告）。
- 2026-09-11：D-111（数据申请批准＋侦察标准：书面间距／三件套齐／优先 Visium 人瘤／先 1–2 块／解锁判据；下载另批）。
- 2026-09-11：四线索＋HTAN＋Cervilla 侦察完成；Cervilla CRC 对经用户明确批准下载并 md5 验对；其余未下载。
- 已知瑕疵：`/tmp` 下侦察中间文件（XML／xlsx／API JSON）未进仓库，全部可重下（§5 给出来源）；triplet 顺序未解（§6 B）。
