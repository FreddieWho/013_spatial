## 我们在回答什么问题
我们在判断：没有清晰边界或现成名称的组织区域里，分子信号能否形成可重复、可量化、可重叠的连续空间场。已知结构只用来验证其中一部分场，不决定我们能发现什么。

## 目前知道了什么
`K_model` 是模型允许的潜在空间效应维度，`K_eff` 才是数据在留出预测、重启稳定性和空间 null 等证据下支持的有效维度；二者都不是生物结构数量。当前 `selected_k=null`。今天的关键进展：旧 TensorFlow 时代发现的"不同数据折对 K 的偏好相反"现象，在 PyTorch 迁移后完整复现——fold 0 偏 K=0（−28.96），fold 4 偏 K=3（+72.73，六名患者中五名更优），方向与旧结果一致。这说明该异质性是数据的性质，不是代码实现的伪影。一个有价值的线索：在 K=3 获胜的 fold 4 里模型实际用了约 2 个有效空间方向，在 K=0 获胜的 fold 0 里只用约 1 个——fold 间差异可能就在有效空间维度上，但这只是单次运行的观察。K 的取舍仍未裁决，也不需要现在裁决。

## 今天做了什么
凌晨先完成了 fold 0 的收尾对比与归档并退租了第一台实例（花费 ¥3.88）。随后把 fold 4 的全部 GPU 作业预置成可复跑脚本包，用户租好第二台同规格实例后执行：上传数据、装环境、数据完整性校验（三个指纹与历史完全一致）、约 1 小时跑完 fold 4 的 K=0/K=3 配对实验和端点审计，产物已回传本地。期间修了两处脚本问题（上传目录层级、校验字段位置），均已固化进作业手册。

## 接下来做什么
两个 fold 的配对证据齐了，按既定顺序进入下一步：在 Torch 结果上做"结构读出对 K 是否敏感"的判断——如果主要结构结论不依赖那个不稳定的空间方向，K=3 作为工程工作表示即告成立，K 问题关闭；只有依赖时才触发 K=2 的补充实验。空间置换 null、组成—状态拆分和独立 lineage 复现继续暂停。
