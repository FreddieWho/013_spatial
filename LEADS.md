# LEADS（探索线索，只追加；状态取 待挖掘 / 已放弃（原因） / 已并入主线）

2026/9/5
## L-001 K=2 Stage A（Torch fold-0/4 新 fit+推断，`--k-values 0,2`）
D-100 门禁曾按预注册规则判 SENSITIVE；D-101 转探索定位后重判为描述性未决，K=2 从触发门禁降为可选探索。
若执行，其 6 患者/fold 留出分数可对第三方向做有检验力裁决（GT 读出的 N=2 配对患者限制仍在，裁决主力是分数对照不是读出）。
最小代价：两次配对跑，约 ¥3–8、一晚（见 infra/r04/GPU_RUNBOOK.md）；执行需单独 GPU 审批，不自动租用。
状态：已并入主线（2026-09-10 K=2 Stage A 执行完毕，见 D-106）

2026/9/5
## L-002 组成拆分：缺逐 spot 组成估计与单细胞参考
状态：已并入主线（2026-09-10三线并行：marker代理＋GSE236581参考双路线执行中）
本地 panel cache 只有 counts 矩阵，无反卷积产物、无已登记单细胞参考；`r04/composition.py` 只有方法无输入。
marker-gene 代理路线与 R-02（表达派生标签禁令）冲突，需单独决策且只能标探索级；正式路线需外部参考（要走外部数据审批）。
最小验证：先定参考与方法（用户拍板），再做单 fold smoke。（2026-09-10 已拍板，双路线执行中，见上）

2026/9/10
## L-003 NNLS 均值反卷积（已判死，不再投入）
全基因与 DE 限定两版下免疫份额均与经典 marker 零/反相关（LYZ–Mye −0.19，PTPRC–T −0.04）；T/ILC 均值 profile 余弦 0.991，加平台 gap，方法假设不成立。脚本与参考保留为负结果资产。
状态：已放弃（方法假设不成立，marker-proxy 路线替代）

2026/9/14
## L-004 浆细胞模块跨患者符号翻转（TLS 信号患者异质性）
阳性对照中同一浆模块（IGHM/IGKC/JCHAIN/MZB1/SDC1/IGLC3/IGHA2）在 0bd3 的 TLS 上 AUC 0.81，在 a29 两片上 0.34–0.38（反向）。
若成立，TLS 的代理信号至少分两类患者生态（浆细胞富集型 vs 贫乏型），单签名模型注定跨患者失败；与 H-05 的条件性偏移主张直接相关。
最小验证：registry 回填时把浆模块 AUC 作为 TLS 行的逐患者列，看分布是否双峰；零成本，数据已在手。
状态：待挖掘

2026/9/14
## L-005 panel 再设计：为 TLS 类目标补足经典标记
现有 4000 基因 panel 缺 CXCL13/CD20/CD3/CCL19/CCL21/LTB 等全部经典 TLS 基因（I-020），TLS 读出只能靠组成型代理（PTPRC/浆模块/非上皮）。
若 TLS 未来成为 claim 目标，第一步应是重选或扩 panel 并先查标记覆盖，而非直接上模型；若 panel 不能动，TLS 只能以组成型场登记，残余维度应记 NOT_TESTABLE。
最小验证：拿候选新 panel 的标记覆盖率清单（各目标结构的经典标记命中数）做一次文书审查，约半天。
状态：待挖掘

2026/9/14
## L-006 marker-overlap 匹配：救回被毛发团吞掉的弱特异性 pattern
Tier-2 平均链接普查中，背景毛发团（R16P001/002，跨 28/27 患者、纯度 0.19/0.28）吞掉了弱特异性的真 pattern（B 细胞 6 个 cluster 被吞——它们只在 2 个患者出现，签名被背景稀释后被吸进毛发团）。
若成立，按 top-N 鉴别 marker 的重叠度（而非全签名余弦）匹配，能免疫背景稀释，把这类 pattern 从毛发团里解放出来；B 细胞组是现成的正对照（0.86 的类内相似度证明它们本应成组）。
最小验证：对毛发团成员算 top-30 marker 两两重叠，看 B/浆等类是否聚成独立小簇；CPU 分钟级；签名 npz 本地持久化（git 忽略，可由 census 脚本确定性重建）。
状态：待挖掘

2026/9/15
## L-007 隐含场优先：已注册源周围不预设晕的成分
"切到 TLS 核能量外面晕"的便宜裁决模型太简单（CXCL13/B/T 三件套）。若晕真是这三样，病理医生在 Visium 切片上肉眼早就看见了——值得找的恰恰是肉眼看不见的隐含场（非常规基因、组合、或核外特定距离带的非单调结构）。
若成立，Lane A 在源周围的筛选必须无偏（全基因×函数族），三件套只当阳性对照，不当发现天花板。
最小验证：已注册 TLS 源周围全基因距离谱先跑一遍，看 top 命中的是不是三件套；CPU 小时级，坐标与标注在手。
状态：待挖掘

2026/9/15
## L-008 旋转感知 PC 匹配：救回被拆分到不同 PC 的共享程序
Tier-1 v2 只做 loading 向量的符号对齐。同一生物学程序在 A 片是 PC3、在 B 片被拆成 PC2＋PC5 时，逐向量 |cos| 匹配必然漏检——当前复发检验对此类程序灵敏度不足（中位跨病人相似度 0.306 里可能混着这类漏网者）。
若成立，子空间对齐（Procrustes/典型相关/子空间夹角）能把“同一程序的不同旋转”认出来，NEW 复现轴的阴性结论需按方法升级修订（D-118 失效条件已预告）。
最小验证：在已升级的 R16B_002（基质，3 病人）成员片上试 Procrustes 对齐，看组内 loading 相似度是否系统性高于逐向量 |cos|；CPU 分钟级，loading 已持久化（git 忽略，可重算）。
状态：待挖掘

2026/9/18
## L-008 状态更新（恢复包 T1 承接）：有边界子空间诊断
原"旋转感知 PC 匹配"线索转 T1 任务 4：已知基质组＋组成正对照＋至多 3 个修复后未解释候选，在相同基因度量下 QR/SVD 主角度/投影回收；只验"同一空间不同基底"，不以子空间重合证同一生物场。T1 交付后本条按结果改状态。
状态：已并入主线（T1）

2026/9/18
## L-007 状态更新（恢复包 T3/T4 承接）：非预设场目标
原"隐含场优先"由 T3 程序整理＋T4 独立验证承接；L-007 本体保留作目标提醒，不再是悬空线索。
状态：已并入主线（T3/T4）

2026/9/18
## L-005 备注：旧 4000 panel 与新 10k 覆盖不是一回事
L-005 的 panel 盲区结论基于旧 4000 panel（I-020）；当前发现宇宙为 HVG-10k（D-114，14/14 TLS 基因救回）。TLS 若成 claim 目标仍需查 10k 内经典标记覆盖，不得直接引用旧 panel 结论。
状态：待挖掘（已备注口径差异）

2026/9/19
## L-009 P-mhc2 的 TLS 指向验证（T5B 最强线索的后续）
P-mhc2 在 6 可评片 5 片≥B 轴（LC3 0.67>0.54 等），且 T4-M2 独立双命中。但阳性窗仅 1–3 个/片（8×8 窗太粗），精度 0.1–0.2。
若成立，抗原呈递活性程序可作 TLS 存在性检测的组成外增量，直指 H-02 的存在性分支。
最小验证：更细遮蔽窗重跑 T5B，或独立 TLS 队列复检"超 B 轴"；约半天 CPU。
状态：待挖掘

2026/9/19
## L-010 T2P2 主 surrogate（Lane A 关联 p 的最终裁决）
T2-small 只验机械，主 surrogate（变异函数匹配，不规则几何无精确解）未做，关联 p 全 NOT_CALIBRATED。
若做出来，Lane A 可重判；若确认做不出，Lane A 永久记 NOT_CALIBRATED，距离函数问题由 T4 式独立预测回答。
最小验证：先对程序分（非全 10k 基因）试变异函数匹配，看校准曲线是否可用；CPU 天级。
状态：待挖掘

2026/9/19
## L-009 关闭（D-134）：mhc2超B轴不成立
细窗963配对下B轴显著更好（p=3.3e-08），三档窗/8片/大小foci一致。T5B小n估计被推翻；T4活性证据不受影响。
状态：已放弃（细窗验证失败）

2026/9/20
## L-011 peri-TLS 晕环形态（spline 新发现，描述性）
LC3/LC4 的 mhc2 曲线呈近TLS峰→谷→远端回升（LC3: 1.70→0.66→1.12），B轴同片平坦。
mhc2 振幅数倍于 B 轴——TLS 周围有 B 丰度之外的空间结构，但不是单调梯度。
若成立，mhc2 的"组织化信息"以晕环而非梯度形式存在，与 L-009（hit/miss 输 B 轴）不矛盾：
hit/miss 只问"阳性窗分高不高"，晕环的谷恰好拉低阳性窗均值。
最小验证：ST-CRC 用 B 轴代理锚点复检晕环＋更多 USZ 片；约半天 CPU。
状态：待挖掘
